project 4

working with n ary trees,
tree traversal algos
search tree and insert nodes in various locations
using std

inserting counters in code. inserting a peice of code for every line.

can figure out run time behavior.
can point out bottle necks and tell what lines get executed how many times

in oder to do that, must parse c++ identify functions, and statements to profile individual statements

need to build an abstract syntax tree:

building a profiler:
need to automatically insert counters to keep track od how many times a statement is ececuted

cout is a method to increment
__LINE__ is a c++ macro to get the line number

what is needed?
find all statements to be profiled; count expression statements(calls, assignmetns, operations)
need to parse source code to find all statements
results in abstract syntax tree (AST)
search tree for statements to profile
insert node

parsing c++ (Dont want to do, very difficult)
using predefined parsing tool, (srcML, generates an AST in XML)
Developed by Maletic and friend

AST:
tree representation of the syntactic expression of the source code
based on grammer of language
describes syntactic category of each language construct

void foo(){
    ++x;
}

					function
		type		name		parameter list		block
		name   		foo		  parameter	     block content
		void				     decl		expr-statement
						type	name		operator   name   t




XML ~ HTML aka a markup language
<function>
	<type><name>foo<><>
	<name>foo<>
	<paramlist>
		<parameter>
			<decl>
				<type><name>int<><>
				<name>x<>
			<>
		<>
	<>
	<block>
		<expr_stat><expr>
			<operator>++<>
		<><>
			
PROJECT 4:
implemented: 
    read in 1 or more srcML documents
    builds internal tree representation of the AST
    write out the AST as source code

// to run ./profiler [XML file]
need to complete:
    read and understand provided code
    build the copy ctor and dtor <M1>
    traverse tree to find specific statements 
    insrument code by inserting new nodes